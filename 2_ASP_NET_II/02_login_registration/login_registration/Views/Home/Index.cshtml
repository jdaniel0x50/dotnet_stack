@{
    ViewData["Title"] = "Home Page"; 
}

@{
    string valUsername = "";
    string valFirstName = "";
    string valLastName = "";
    string valEmail = "";
    string valBirthdate = "";
    string valPassword = "";

    if(ViewBag.errors != null)
    {
        valUsername = ViewBag.errors["Username"].RawValue;
        valFirstName = ViewBag.errors["FirstName"].RawValue;
        valEmail = ViewBag.errors["Email"].RawValue;
        valLastName = ViewBag.errors["LastName"].RawValue;
        valBirthdate = ViewBag.errors["Birthdate"].RawValue;
        valPassword = ViewBag.errors["Password"].RawValue;
    }

    <div id="fullscreen_bg" class="fullscreen_bg"></div>
    <div id="regContainer" class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3 mx-auto">
                <div class="panel panel-login p-3">
                    <div class="panel-heading">
                        <div class="row mx-auto">
                            <div class="col-xs-6 mx-auto">
                                <a href="#" class="active" id="register-form-link">Register</a>
                            </div>
                            <div class="col-xs-6 mx-auto">
                                <a href="#" id="login-form-link">Login</a>
                            </div>
                        </div>
                        <hr class="mt-2 mb-3">
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-lg-12">
                                <form id="register-form" action="/register" method="post" role="form" style="display: block;">
                                    <!-- registration form -->
                                    <div class="form-group">
                                        <label for="Username">Username</label>
                                        <input type="text" name="Username" tabindex="1" class="form-control" placeholder="Username" value="@valUsername">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "Username" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <label for="FirstName">First Name</label>
                                        <input type="text" name="FirstName" tabindex="2" class="form-control" placeholder="First Name" value="@valFirstName">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "FirstName" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <label for="LastName">Last Name</label>
                                        <input type="text" name="LastName" tabindex="3" class="form-control" placeholder="Last Name" value="@valLastName">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "LastName" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <label for="Email">Email</label>
                                        <input type="text" name="Email" tabindex="4" class="form-control" placeholder="email@domain.ext" value="@valEmail">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "Email" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <label for="Birthdate">Birthdate</label>
                                        <input type="date" name="Birthdate" tabindex="5" class="form-control" placeholder="mm/dd/yyyy" value="@valBirthdate">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "Birthdate" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <label for="Password">Password</label>
                                        <input type="password" name="Password" tabindex="6" class="form-control" placeholder="Password" value="@valPassword">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "Password" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <label for="ConfirmPassword">Confirm Password</label>
                                        <input type="password" name="ConfirmPassword" tabindex="7" class="form-control" placeholder="Confirm Password" value="">
                                    </div>
                                    <div>
                                        @{
                                            if(ViewBag.errors != null)
                                            {
                                                @foreach(string key in ViewBag.errors.Keys)
                                                {
                                                    if (key == "ConfirmPassword" && ViewBag.errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>
                                    <div class="form-group">
                                        <div class="row">
                                            <div class="col-sm-6 mx-auto">
                                                <input type="submit" name="register-submit" id="register-submit" tabindex="8" class="form-control btn btn-register" value="Register Now">
                                            </div>
                                        </div>
                                    </div>
                                </form>
                                <form id="login-form" action="/login" method="post" role="form" style="display: none;">
                                    <div class="form-group">
                                        <label for="Username">Username</label>
                                        <input type="text" name="Username" tabindex="1" class="form-control" placeholder="Username" value="">
                                    </div>
                                    <div class="form-group">
                                        <label for="Password">Password</label>
                                        <input type="password" name="Password" tabindex="2" class="form-control" placeholder="Password">
                                    </div>
                                    <div class="form-group">
                                        <div class="row">
                                            <div class="col-sm-6 mx-auto">
                                                <input type="submit" name="login-submit" id="login-submit" tabindex="3" class="form-control btn btn-login" value="Log In">
                                            </div>
                                        </div>
                                    </div>
                                    <!-- validations for the login form -->
                                    <div>
                                        @{
                                            if(ViewBag.login_errors != null)
                                            {
                                                foreach(string key in ViewBag.login_errors.Keys)
                                                {
                                                    if (key == "login" && ViewBag.login_errors[key].Errors.Count > 0)
                                                    {
                                                        foreach(var error in ViewBag.login_errors[key].Errors)
                                                        {
                                                            <p class="text-danger font-weight-bold">@error.ErrorMessage</p>
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    </div>

                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}